# Archived experiments — not used in the live app.

- `hero/` — old hero variants (3D logo, particles, pixel grid)
- `backgrounds/` — unused background shaders
- `cursor/` — splash cursor effect
- `shaders/` — terrain shader

Do not import from here in production code.
